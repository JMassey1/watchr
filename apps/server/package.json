{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.mjs",
    "clean": "rm -rf node_modules dist .turbo .cache"
  },
  "dependencies": {
    "@hono/trpc-server": "^0.4.2",
    "@trpc/server": "catalog:",
    "@watch3r/api": "workspace:*",
    "@watch3r/auth": "workspace:*",
    "@watch3r/db": "workspace:*",
    "@watch3r/env": "workspace:*",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "hono": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "@watch3r/config": "workspace:*",
    "tsdown": "^0.22.3",
    "typescript": "^6"
  }
}
